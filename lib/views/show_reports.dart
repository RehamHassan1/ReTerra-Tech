import 'package:flutter/material.dart';

import 'show_report_body.dart';

class ShowReport extends StatelessWidget {
  const ShowReport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ShowReportBody() ,
    );
  }
}