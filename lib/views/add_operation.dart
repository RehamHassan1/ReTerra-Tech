import 'package:flutter/material.dart';

import 'add_operation_body.dart';

class AddOperation extends StatelessWidget {
  const AddOperation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: AddOperationBody() ,);
  }
}