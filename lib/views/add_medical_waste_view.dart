import 'package:flutter/material.dart';

import 'add_medical_waste_body.dart';

class AddMedicalWasteView extends StatelessWidget {
  const AddMedicalWasteView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: AddMedicalWasteViewBody(),);
  }
}